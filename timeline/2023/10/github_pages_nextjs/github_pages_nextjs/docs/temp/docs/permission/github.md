Owner（所有者）：拥有对仓库的完全控制权限，包括修改仓库的设置、管理团队成员、删除仓库等。

Admin（管理员）：可以管理仓库的设置、管理团队成员，但不能删除仓库。

Write（写入）：可以推送代码、管理问题和拉取请求，但不能管理仓库设置。

Read（读取）：只能查看代码和问题，不能进行任何修改。

Triage（整理）：可以管理问题和拉取请求，但不能推送代码。

Maintain（维护）：可以管理仓库的设置和团队成员，但不能删除仓库。